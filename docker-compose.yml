version: "3.8"

services:
  # Base de Datos PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: autovideo_postgres
    restart: always
    environment:
      POSTGRES_USER: autovideouser
      POSTGRES_PASSWORD: autovideopass
      POSTGRES_DB: autovideodb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autovideouser -d autovideodb"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Message Broker Redis (Para Celery)
  redis:
    image: redis:7-alpine
    container_name: autovideo_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel warning
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
  redis_data:
